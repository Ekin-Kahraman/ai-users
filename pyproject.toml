[project]
name = "ai-country-usage"
version = "0.1.0"
description = "A research tool for visually exploring estimated generative AI usage by country."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
